SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'stage',
      #directory : 'src',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
	  #ignoreImage : false,
      #platforms : [ #pharo ]
    }
  ]
}
